{
  "name": "claude-sub-proxy",
  "version": "1.0.0",
  "description": "Lightweight proxy that reroutes Claude Code subagent calls to cheaper third-party models while keeping the main model on your Anthropic subscription.",
  "type": "module",
  "bin": {
    "claude-sub-proxy": "./cli.mjs"
  },
  "scripts": {
    "start": "node cli.mjs start",
    "test": "node --test && bash -n install.sh"
  },
  "keywords": ["claude-code", "proxy", "subagent", "minimax", "routing"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
